# vulcan_map

A standalone repository, nested as a plain folder inside the SpaceAGORA.jl working tree (not a git submodule).

This is separate from `.vulcan/`, the working directory used by the Vulcan autonomous coding agent when it operates on SpaceAGORA.jl (requirements, architecture sheets, run logs, and V&V artifacts for scoped agent tasks against that codebase).

Purpose and contents to be defined.
